apiVersion: v1
kind: Service
metadata:
  name: public-service
spec:
  type: NodePort   # easiest for local testing
  selector:
    app: public
  ports:
    - port: 8080        # cluster port
      targetPort: 8080  # container port
      nodePort: 30080   # external port on node